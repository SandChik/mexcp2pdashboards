// Bumped with every release. Compared against the backend's /health version in
// Settings so a stale browser build or a stale server build is obvious at a
// glance instead of needing an SSH session to diagnose.
export const APP_VERSION = 'v47';
